# OpenRouter / 自部署 LLM Key；若仅离线使用本地权重可留空
OPENROUTER_API_KEY=
# HuggingFace 读取私仓或高并发需填
HF_TOKEN=
# Redis 连接串（Homebrew 默认）
REDIS_URL=redis://localhost:6379/0
# 指定本地下载后的模型目录，留空则默认 ~/.cache/huggingface
MODEL_DIR=
